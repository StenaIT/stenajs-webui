import { StandardTableVariant } from "../components/StandardTable";

export const elementHeightPerVariant: Record<StandardTableVariant, number> = {
  relaxed: 48,
  standard: 40,
  condensed: 32,
  compact: 24,
};
