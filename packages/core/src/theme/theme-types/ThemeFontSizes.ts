export interface ThemeFontSizes {
  huge: string;
  larger: string;
  large: string;
  normal: string;
  small: string;
  smaller: string;
  tiny: string;
}

export type ThemeFontSizeField = keyof ThemeFontSizes;
